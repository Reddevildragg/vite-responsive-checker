# Plugins

This directory contains plugins for the application.

Each plugin should reside in its own subdirectory and export a standard Vue plugin.

## Example Plugin

The `example-plugin` demonstrates how to structure a plugin.
